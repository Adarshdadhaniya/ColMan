<!DOCTYPE html>
<html>
<head>
  <title>Sent Requests</title>
</head>
<body>
  <h1>Sent Exchange Requests</h1>
  <% if(requests.length === 0) { %>
    <p>No sent requests.</p>
  <% } %>
  <ul>
    <% requests.forEach(r => { %>
      <li>
        To: <%= r.receiver.username %> | <%= r.day %> | <%= r.timeSlot %> | <%= r.classGroup %>
        Status: <%= r.status %>
      </li>
    <% }) %>
  </ul>
</body>
</html>
